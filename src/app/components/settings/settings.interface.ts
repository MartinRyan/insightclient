export interface Settings {
  timeRange: number;
  accessToken: string;
  gitlabAddress?: string;
  namespace?: any;
  subgroup?: any;
  perPage: number;
  numberOfDaysGrid: number;
  numberOfDaysHeatmap: number;
}
